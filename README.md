# skills

Iliyas's personal agent skills, installable into any project with
[`skills`](https://github.com/vercel-labs/skills):

```bash
npx skills add iliyasone/skills          # picks up gh auth automatically
```

Each skill lives in `skills/<name>/SKILL.md`. The repo is private: whoever can
read it can install from it.
